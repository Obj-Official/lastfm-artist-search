# LastFM Artist Search

An optimized app that searches the lastfm api for artists

### Prerequisites
- Node.js 18+ installed
- Backend API running (default: http://localhost:3198)

## Features
To run, enter the command **Node server.js** in your cmd
In your browser enter the link  http://localhost:3198?artist=artistname to search for artist
